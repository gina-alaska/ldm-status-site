-@ldm.queues.each do |name, queue|
  .well
    %p          
      .lead
        =name
    -if queue.files.any? 
      %dl.dl-horizontal
        %dt="Last Item"
        %dd=queue.files.last.basename
        %dt="Time Added"
        %dd=queue.files.last.mtime
        %dt="Size"
        %dd
          -if queue.files.last.size > 1024 * 1024
            = "#{queue.files.last.size / (1024*1024)}M"
          -else   
            = "#{queue.files.last.size / 1024}K"
    - else
      No items in queue